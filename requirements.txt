# Flask web server + CORS
flask
flask-cors

# Environment variable loading
python-dotenv

# LangChain ecosystem
langchain
langchain-community
langchain-text-splitters
langchain-nvidia-ai-endpoints

# Document loaders (PDF, DOCX, PPT)
unstructured
unstructured[pptx]
pypdf
docx2txt

# Chroma vector database
chromadb

# Sentence embedding model
sentence-transformers

# (Optional but recommended: ensures compatibility with langchain components)
tiktoken
requests
numpy
